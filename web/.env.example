# Leave empty in local `npm run dev` so Vite proxies /api to the Node server.
# For a production static host, set the public API origin:
# VITE_API_BASE_URL=https://api.example.com
VITE_API_BASE_URL=
